package com.example.android.boomplacer.game.ui

import android.app.Activity
import com.example.android.boomplacer.game.GameFlow

class StartingMenu(private val activity: Activity, private val gameFlow: GameFlow, private val userInterface: UserInterface) {
    fun hide () {}
}